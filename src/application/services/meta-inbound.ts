/**
 * @deprecated Import from `@/application/services/channel-inbound` instead.
 * Kept as a thin re-export for any leftover imports.
 */
export {
  resolveChannelForInbound,
  processChannelInboundMessage,
  processMetaInboundMessage,
  type ResolvedChannel,
} from "./channel-inbound";
